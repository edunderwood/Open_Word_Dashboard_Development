# OpenWord Dashboard Environment Configuration

# Server Configuration
NODE_ENV=production

# Supabase Configuration (same as main OpenWord server)
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Supabase Management API (for billing/plan info on Costs page)
# Get token from: supabase.com/dashboard/account/tokens
# Get org ID from URL: supabase.com/dashboard/org/YOUR_ORG_ID/...
SUPABASE_ACCESS_TOKEN=sbp_your_personal_access_token
SUPABASE_ORG_ID=your_organization_id

# Stripe Configuration (same as main OpenWord server)
STRIPE_SECRET_KEY=your_stripe_secret_key

# Session Configuration
SESSION_SECRET=your_session_secret_here

# Dashboard URL (for password reset redirects)
DASHBOARD_URL=https://open-word-dashboard-development.onrender.com

# Email Notifications (for critical alerts)
# Using SendGrid (recommended) - https://sendgrid.com
# 1. Sign up at sendgrid.com (free tier: 100 emails/day)
# 2. Go to Settings → API Keys → Create API Key (Full Access)
# 3. Copy the key (starts with SG.)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.your_sendgrid_api_key_here
ALERT_EMAIL=support@openword.live
SENDER_EMAIL=alerts@yourdomain.com

# Monitoring Configuration
MONITOR_INTERVAL_MINUTES=5
ALERT_THRESHOLD_ERROR_RATE=10
ALERT_THRESHOLD_RESPONSE_TIME_MS=5000

# OpenWord Main Server URL (for health checks)
OPENWORD_SERVER_URL=https://openword.onrender.com

# Analytics API Keys
# Deepgram (for speech-to-text usage analytics)
DEEPGRAM_API_KEY=your_deepgram_api_key
DEEPGRAM_PROJECT_ID=your_deepgram_project_id

# Render.com (for server hosting analytics)
RENDER_API_KEY=your_render_api_key

# Google Cloud (for Translation API analytics)
# Option 1: Set credentials as JSON string (recommended for cloud deployments)
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key":"..."}
# Option 2: Use file path (for local development)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/google-api-credentials.json
GOOGLE_CLOUD_PROJECT_ID=your_google_cloud_project_id

# Vercel (for client hosting analytics)
VERCEL_TOKEN=your_vercel_token
VERCEL_TEAM_ID=your_vercel_team_id

# OSCR API (Scotland Charity Register)
# Used for syncing Scottish charity data from the OSCR API
OSCR_API_KEY=your_oscr_api_key

# Charity Commission API (England & Wales)
# Get API key from: https://api-portal.charitycommission.gov.uk/
CHARITY_COMMISSION_API_KEY=your_charity_commission_api_key
